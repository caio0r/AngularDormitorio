import { CamaFiltroPipe } from './cama-filtro.pipe';

describe('CamaFiltroPipe', () => {
  it('create an instance', () => {
    const pipe = new CamaFiltroPipe();
    expect(pipe).toBeTruthy();
  });
});
